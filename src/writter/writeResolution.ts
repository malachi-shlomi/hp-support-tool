import { CaseProps } from "../lib/interfaces";

const writeResolution: (caseProps: CaseProps) => string = (caseProps) => {
    let resolution = ''

    return resolution;
}

export default writeResolution;